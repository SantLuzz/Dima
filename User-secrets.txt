//cria uma referência segura que não é versionada e nem vai para o github, fica somente na maquina local

//para carregar basta usar o GetConnectionString como se fosse buscar do appSettings.

//a estrutura da chave deve ser a mesma do appSettings porem aninhando os itens usando dois pontos (:)

//inicia
dotnet user-secrets init

//cria o segredo - nesse exemplo crio para a string de conexão
dotnet user-secrets set "ConnectionStrings:DefaultConnection" "Server=localhost,1433;Database=dima-dev;User ID=sa;Password=1436329501Feeh@;Trusted_Connection=False; TrustServerCertificate=True;"

//limpa todos os segredos
dotnet user-secrets clear